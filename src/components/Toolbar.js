import React from 'react';

function Toolbar() {
    return (
        <div className="toolbar">
            <button>Add Text</button>
            <button>Add Image</button>
            {/* Other design tools go here */}
        </div>
    );
}

export default Toolbar;